node_modules/
dist/
dev-dist/
coverage/
.env
.env.*
!.env.example
*.log
.DS_Store
playwright-report/
test-results/
e2e/.auth/

# Verification screenshots. Agents write these to prove a screen renders;
# they are evidence for a review, not source, and 5.7MB of PNGs does not
# belong in git history.
shots/
